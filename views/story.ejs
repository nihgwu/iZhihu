<% include head %>
<!-- Content -->
<section id="wrapper">
  <%- story.body %>
  <div id="pager-wrapper">
    <div id="pager">
      <% if(pre) { %>
      <a href="<%= pre.url %>" title="<%= pre.title %>" ><%= pre.text||'后一篇' %></a>
      <% } else { %>
      <span>没有了</span>
      <% } %>
      ·
      <% if(next) { %>
      <a href="<%= next.url %>" title="<%= next.title %>" ><%= next.text||'前一篇' %></a>
      <% } else { %>
      <span>没有了</span>
      <% } %>
    </div>
  </div>
</section>
<div id="share">
  <script>
    window._bd_share_config = {"common": {"bdSnsKey": {}, "bdText": "", "bdMini": "1", "bdMiniList": ["sqq", "weixin", "tsina", "tqq", "qzone", "douban"], "bdPic": "", "bdStyle": "0", "bdSize": "16"}, "slide": {"type": "slide", "bdImg": "3", "bdPos": "right", "bdBottom": "150"}};
    with (document)0[(getElementsByTagName('head')[0] || body).appendChild(createElement('script')).src = 'http://bdimg.share.baidu.com/static/api/js/share.js?v=89860593.js?cdnversion=' + ~(-new Date() / 36e5)];
  </script>
</div>
<% include foot %>